const auth = {}
const reducer = (state = auth, action) => {
    switch (action.type) {
        case "GET":
            return state;
        default:
            return state;
    }
};
export default reducer